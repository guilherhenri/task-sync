const chalkMock = (text: string): string => text

chalkMock.red = (text: string): string => text
chalkMock.yellow = (text: string): string => text
chalkMock.green = (text: string): string => text
chalkMock.magenta = (text: string): string => text
chalkMock.cyan = (text: string): string => text
chalkMock.blue = (text: string): string => text
chalkMock.gray = (text: string): string => text
chalkMock.white = (text: string): string => text

export default chalkMock
